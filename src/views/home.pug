extends base
include mixins/video
block content
    div.videogrid
        each video in videos
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            +video(video)
            
 
        else
            span Nothing found